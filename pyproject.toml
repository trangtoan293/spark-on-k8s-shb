[project]
name = "dbt-project-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dbt-metricflow[dbt-databricks,dbt-spark]==0.10.1",
    "dbt-spark==1.9.0",
]
